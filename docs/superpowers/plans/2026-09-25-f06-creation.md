# F06 — Création de session — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un écran `/new` qui lit un CSV d'étudiants et une config JSON, les prévisualise avec leurs erreurs et avertissements, puis crée la session (config normalisée figée) et navigue vers `/session/$sessionId`.

**Architecture:** `src/students/` (pur) lit le CSV avec PapaParse et produit des issues structurées traduites à part (D18). `src/sessions/build-session.ts` (pur) construit la `Session`. `src/create/` porte l'UI : un hook de formulaire (`use-create-form.ts`) qui lit les fichiers, charge `validateConfig` à la demande et crée la session via `@/db`, des zones de dépôt réutilisables, deux blocs d'aperçu et la page. Le plugin Vite publie aussi un CSV d'exemple.

**Tech Stack:** React 19, TanStack Router (hash), shadcn v4 base-ui, Zod 4, PapaParse 5, Dexie 4 (`@/db`), Vitest + jsdom + `fake-indexeddb` + Testing Library.

**Spec:** `docs/superpowers/specs/2026-09-25-f06-creation-design.md` (décisions D54–D57 ; aussi D11, D15, D18, D25, D46, D50, D51).

## Global Constraints

- Dépendances ajoutées : `papaparse` (dependencies) et `@types/papaparse` (devDependencies), dernières versions stables. Rien d'autre.
- `src/students/` et `src/sessions/` : imports **relatifs** (`../domain`, `../config`, `../i18n`, `../app-version`), ni React ni `@/db`.
- Toute chaîne visible (y compris `aria-label`) passe par `UI_MESSAGES` (fr **et** en) ou par `formatCsvIssue` / `formatConfigIssue`. Aucun texte en dur.
- Composants : props en `Readonly<{…}>` (Sonar S6759), jamais `const [x] = useState(...)` sans setter (S6754), `useUi()` une seule fois au composant de page et `ui: Ui` passé en prop. `DialogContent` éventuel : `showCloseButton={false}`. Ne jamais modifier `src/components/ui/`.
- `validateConfig` est chargé en `import('@/config/validate')` (convention « Module lourd chargé à la demande ») ; l'écran n'importe jamais `@/config/validate` statiquement.
- Couleurs uniquement via tokens (`text-destructive`, `text-muted-foreground`…).
- Aucune assertion `as` hors motif déjà établi, pas de ternaire imbriqué, pas d'`expect` conditionnel, `items.map((x) => f(x))` quand `f` a un 2ᵉ paramètre (S7727), pas de regex super-linéaire (S5852).
- Tests : `*.test.ts(x)` à côté du code, noms en français ; tests touchant la base : `import 'fake-indexeddb/auto'` en première ligne, `beforeEach(() => db.sessions.clear())` avec `db` de `@/db/db`. `src/test/setup.ts` force `navigator.languages = ['fr-FR']` et `asyncUtilTimeout: 5000`.
- Test ciblé : `./node_modules/.bin/vitest run <fichier>` — jamais `pnpm vitest`. Fin de tâche : `pnpm check`. Tâches 2 et 5 : aussi `pnpm build`, sans avertissement de taille de chunk.
- Ne jamais lancer `pnpm dev` (un serveur partagé peut tourner, QUIRKS 2026-09-25).
- Commits gitmoji, message au présent en français, emoji Unicode, pied :
  ```
  Co-Authored-By: <modèle qui écrit le commit> <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_016w1nyky4gGcgYAfqw2Z9YW
  ```
- Branche `feat/f06-creation` (déjà créée, spec commitée).

## Review Focus

1. **CSV exporté par Excel FR** (BOM + `;` + CRLF + ligne vide finale) : lu sans avertissement parasite, numéros de ligne justes. Test : Task 1.
2. **Fichier CSV d'une seule colonne** (`Nom` seul, ou un nom par ligne) : PapaParse lève `UndetectableDelimiter` ; ce n'est pas une erreur de syntaxe, chaque ligne devient `single_field_row`, puis `no_students`. Test : Task 1.
3. **Remplacer la config par une config invalide après préremplissage du nom** : le nom reste, « Créer » se désactive. Test : Task 4.
4. **Double clic sur « Créer »** : une seule session créée. Test : Task 4.
5. **Config valide avec avertissements** (icône inconnue) : création autorisée, avertissements affichés. Test : Task 5.

---

### Task 1: lecture du CSV (`src/students/`)

**Files:**
- Create: `src/students/header.ts`, `src/students/issues.ts`, `src/students/parse-csv.ts`, `src/students/parse-csv.test.ts`, `src/students/messages.ts`, `src/students/messages.test.ts`, `src/students/index.ts`
- Modify: `package.json`, `pnpm-lock.yaml` (ajout papaparse)

**Interfaces:**
- Consumes: `Dictionary`, `Locale`, `t` (`../i18n`).
- Produces:
  - `type CsvStudent = { lastName: string; firstName: string; line: number }`
  - `type CsvParseResult = { students: CsvStudent[]; issues: CsvIssue[] }`
  - `parseStudentsCsv(text: string): CsvParseResult`
  - `type CsvIssueParams`, `type CsvIssueCode`, `type CsvIssue = { severity: 'error' | 'warning'; code; line?: number; params }`
  - `CSV_ISSUE_MESSAGES`, `formatCsvIssue(issue: CsvIssue, locale: Locale): string` (message sans « Ligne N : » ; l'UI préfixe quand `issue.line` est défini)
  - `normalizeHeaderCell(cell: string): string`, `classifyHeaderCell(cell: string): 'lastName' | 'firstName' | undefined`

**Précision par rapport au tableau de la spec** : le numéro de ligne est porté par `issue.line`, pas répété dans `params`. Donc `csv_syntax`, `single_field_row` : `params: {}` + `line` ; `duplicate_student` : `params: { firstLine, name }` + `line` ; `preamble_skipped` : `{ count }` ; `extra_columns` : `{ count }` ; `no_students` : `{}`.

- [ ] **Step 1: dépendance** — `corepack pnpm add papaparse` puis `corepack pnpm add -D @types/papaparse`.

- [ ] **Step 2: tests qui échouent** — `src/students/parse-csv.test.ts` :

```ts
import { describe, expect, test } from 'vitest'
import { parseStudentsCsv } from './parse-csv'

const BOM = '﻿'

function names(text: string) {
  return parseStudentsCsv(text).students.map((s) => `${s.lastName} ${s.firstName}`)
}
function codes(text: string) {
  return parseStudentsCsv(text).issues.map((issue) => issue.code)
}

describe('parseStudentsCsv', () => {
  test('export Excel FR : BOM, ;, CRLF, ligne vide finale', () => {
    const result = parseStudentsCsv(`${BOM}Nom;Prénom\r\nDurand;Alice\r\nLefèvre;Chloé\r\n\r\n`)
    expect(result).toEqual({
      students: [
        { lastName: 'Durand', firstName: 'Alice', line: 2 },
        { lastName: 'Lefèvre', firstName: 'Chloé', line: 3 },
      ],
      issues: [],
    })
  })

  test('virgule sans en-tête : nom puis prénom', () => {
    expect(names('Durand,Alice\nMartin,Bruno')).toEqual(['Durand Alice', 'Martin Bruno'])
  })

  test('en-tête anglais en ordre inversé', () => {
    expect(names('First Name,Last Name\nAlice,Durand')).toEqual(['Durand Alice'])
  })

  test.each(['NOM;PRÉNOM', 'nom de famille;prenom', 'Last_Name;given-name', 'Surname;FirstName', ' Family Name ; Prénom '])(
    'en-tête reconnu : %s',
    (header) => {
      expect(parseStudentsCsv(`${header}\nDurand;Alice`).students).toEqual([
        { lastName: 'Durand', firstName: 'Alice', line: 2 },
      ])
    },
  )

  test('préambule de deux lignes ignoré avec un avertissement', () => {
    const result = parseStudentsCsv('Liste BTS SIO 2\nAnnée 2026\n\nNom;Prénom\nDurand;Alice')
    expect(result.students).toEqual([{ lastName: 'Durand', firstName: 'Alice', line: 5 }])
    expect(result.issues).toEqual([{ severity: 'warning', code: 'preamble_skipped', params: { count: 2 } }])
  })

  test("en-tête en 6ᵉ ligne utile : non reconnu, tout est donnée", () => {
    const text = ['a;b', 'c;d', 'e;f', 'g;h', 'i;j', 'Nom;Prénom', 'Durand;Alice'].join('\n')
    const result = parseStudentsCsv(text)
    expect(result.students).toHaveLength(7)
    expect(result.issues).toEqual([])
  })

  test('colonnes en trop : un seul avertissement avec le nombre de lignes', () => {
    const result = parseStudentsCsv('Nom;Prénom;Groupe\nDurand;Alice;A\nMartin;Bruno;B')
    expect(result.students).toHaveLength(2)
    expect(result.issues).toEqual([{ severity: 'warning', code: 'extra_columns', params: { count: 2 } }])
  })

  test('ligne à un seul champ ignorée, avec son numéro de ligne (lignes vides comptées)', () => {
    const result = parseStudentsCsv('Nom;Prénom\n\nDurand;Alice\nMartin;\n;Bruno')
    expect(names('Nom;Prénom\n\nDurand;Alice\nMartin;\n;Bruno')).toEqual(['Durand Alice'])
    expect(result.issues).toEqual([
      { severity: 'warning', code: 'single_field_row', line: 4, params: {} },
      { severity: 'warning', code: 'single_field_row', line: 5, params: {} },
    ])
  })

  test('doublons après normalisation (casse, accents, espaces) : gardés et signalés', () => {
    const result = parseStudentsCsv('Nom;Prénom\nLefèvre;Chloé\n lefevre ;CHLOE')
    expect(result.students).toHaveLength(2)
    expect(result.issues).toEqual([
      { severity: 'warning', code: 'duplicate_student', line: 3, params: { firstLine: 2, name: 'lefevre CHLOE' } },
    ])
  })

  test('trim et casse conservée', () => {
    expect(names('Nom;Prénom\n  de La Tour ;  Jean-Marc ')).toEqual(['de La Tour Jean-Marc'])
  })

  test('cellule entre guillemets contenant le séparateur', () => {
    expect(names('Nom;Prénom\n"Martin; Jr";Paul')).toEqual(['Martin; Jr Paul'])
  })

  test('guillemet non fermé : erreur csv_syntax, aucun étudiant', () => {
    const result = parseStudentsCsv('Nom;Prénom\n"Durand;Alice\nMartin;Bruno')
    expect(result.students).toEqual([])
    expect(result.issues).toEqual([expect.objectContaining({ severity: 'error', code: 'csv_syntax' })])
  })

  test('une seule colonne : lignes ignorées puis no_students, pas csv_syntax', () => {
    expect(codes('Durand\nMartin')).toEqual(['single_field_row', 'single_field_row', 'no_students'])
  })

  test.each([['vide', ''], ['BOM seul', BOM], ['en-tête seul', 'Nom;Prénom\n']])('%s : no_students', (_label, text) => {
    expect(parseStudentsCsv(text)).toEqual({
      students: [],
      issues: [{ severity: 'error', code: 'no_students', params: {} }],
    })
  })
})
```

(Adapter la mise en forme à Prettier ; si oxlint refuse `test.each` avec lambdas inline, extraire des fonctions nommées comme dans `src/domain/schema.test.ts`.)

- [ ] **Step 3:** `./node_modules/.bin/vitest run src/students/parse-csv.test.ts` → FAIL (module absent).

- [ ] **Step 4: `header.ts` et `issues.ts`**

```ts
// header.ts
/** Comparaison d'en-têtes (D25) : NFD sans diacritiques, minuscules, sans espaces, tirets ni underscores. */
export function normalizeHeaderCell(cell: string): string {
  return cell
    .normalize('NFD')
    .replaceAll(/\p{Diacritic}/gu, '')
    .toLowerCase()
    .replaceAll(/[\s_-]/g, '')
}

const LAST_NAME = new Set(['nom', 'nomdefamille', 'lastname', 'surname', 'familyname'])
const FIRST_NAME = new Set(['prenom', 'firstname', 'givenname'])

export function classifyHeaderCell(cell: string): 'lastName' | 'firstName' | undefined {
  const normalized = normalizeHeaderCell(cell)
  if (LAST_NAME.has(normalized)) return 'lastName'
  if (FIRST_NAME.has(normalized)) return 'firstName'
  return undefined
}
```

```ts
// issues.ts
type NoParams = Record<string, never>

/** Paramètres par code (D18) ; le numéro de ligne est porté par `CsvIssue.line`. */
export type CsvIssueParams = {
  csv_syntax: NoParams
  no_students: NoParams
  preamble_skipped: { count: number }
  single_field_row: NoParams
  extra_columns: { count: number }
  duplicate_student: { firstLine: number; name: string }
}

export type CsvIssueCode = keyof CsvIssueParams

export type CsvIssue = {
  [C in CsvIssueCode]: {
    severity: 'error' | 'warning'
    code: C
    line?: number
    params: CsvIssueParams[C]
  }
}[CsvIssueCode]

function createIssue<C extends CsvIssueCode>(
  severity: 'error' | 'warning',
  code: C,
  params: CsvIssueParams[C],
  line?: number,
): CsvIssue {
  // oxlint-disable-next-line typescript/no-unsafe-type-assertion -- union discriminée distributive construite depuis un C générique, comme createIssue (config/issues.ts).
  return (line === undefined ? { severity, code, params } : { severity, code, line, params }) as CsvIssue
}

export function csvError<C extends CsvIssueCode>(code: C, params: CsvIssueParams[C], line?: number): CsvIssue {
  return createIssue('error', code, params, line)
}

export function csvWarning<C extends CsvIssueCode>(code: C, params: CsvIssueParams[C], line?: number): CsvIssue {
  return createIssue('warning', code, params, line)
}
```

(La clé `line` doit être **absente** quand elle n'a pas de valeur : les tests comparent en `toEqual` strict sur la forme.)

- [ ] **Step 5: `parse-csv.ts`**

```ts
import Papa from 'papaparse'
import { classifyHeaderCell } from './header'
import { csvError, csvWarning, type CsvIssue } from './issues'

export type CsvStudent = { lastName: string; firstName: string; line: number }
export type CsvParseResult = { students: CsvStudent[]; issues: CsvIssue[] }

/** Nombre de lignes non vides examinées pour trouver l'en-tête (D55). */
const HEADER_SEARCH_LINES = 5
const BOM = '﻿'

type Row = { line: number; cells: string[] }
type Columns = { lastName: number; firstName: number }

function findHeader(rows: readonly Row[]): { index: number; columns: Columns } | undefined {
  for (const [index, row] of rows.slice(0, HEADER_SEARCH_LINES).entries()) {
    const lastName = row.cells.findIndex((cell) => classifyHeaderCell(cell) === 'lastName')
    const firstName = row.cells.findIndex((cell) => classifyHeaderCell(cell) === 'firstName')
    if (lastName !== -1 && firstName !== -1) return { index, columns: { lastName, firstName } }
  }
  return undefined
}

/** Clé de doublon : trim, sans diacritiques, sans casse. */
function identityKey(lastName: string, firstName: string): string {
  const fold = (value: string) => value.normalize('NFD').replaceAll(/\p{Diacritic}/gu, '').toLowerCase()
  return `${fold(lastName)}\u0000${fold(firstName)}`
}

/**
 * Lit une liste d'étudiants (§6.1, D25, D55). Pure. Aucune ligne isolée ne bloque : seules une
 * erreur de syntaxe (guillemet non fermé) ou l'absence d'étudiant valide sont des erreurs.
 */
export function parseStudentsCsv(text: string): CsvParseResult {
  const source = text.startsWith(BOM) ? text.slice(BOM.length) : text
  const parsed = Papa.parse<string[]>(source, {
    header: false,
    skipEmptyLines: false,
    delimitersToGuess: [',', ';'],
  })
  const quoteError = parsed.errors.find((error) => error.type === 'Quotes')
  if (quoteError) {
    return { students: [], issues: [csvError('csv_syntax', {}, (quoteError.row ?? 0) + 1)] }
  }

  const rows: Row[] = parsed.data
    .map((cells, index) => ({ line: index + 1, cells: cells.map((cell) => cell.trim()) }))
    .filter((row) => row.cells.some((cell) => cell !== ''))

  const issues: CsvIssue[] = []
  const header = findHeader(rows)
  if (header && header.index > 0) issues.push(csvWarning('preamble_skipped', { count: header.index }))
  const columns: Columns = header?.columns ?? { lastName: 0, firstName: 1 }
  const dataRows = header ? rows.slice(header.index + 1) : rows

  const students: CsvStudent[] = []
  const seen = new Map<string, number>()
  let extraRows = 0
  for (const { line, cells } of dataRows) {
    const lastName = cells[columns.lastName] ?? ''
    const firstName = cells[columns.firstName] ?? ''
    const hasExtra = cells.some(
      (cell, index) => cell !== '' && index !== columns.lastName && index !== columns.firstName,
    )
    if (hasExtra) extraRows += 1
    if (lastName === '' || firstName === '') {
      issues.push(csvWarning('single_field_row', {}, line))
      continue
    }
    const key = identityKey(lastName, firstName)
    const firstLine = seen.get(key)
    if (firstLine === undefined) seen.set(key, line)
    else issues.push(csvWarning('duplicate_student', { firstLine, name: `${lastName} ${firstName}` }, line))
    students.push({ lastName, firstName, line })
  }
  if (extraRows > 0) issues.push(csvWarning('extra_columns', { count: extraRows }))
  if (students.length === 0) issues.push(csvError('no_students', {}))
  return { students, issues }
}
```

Points à vérifier en implémentant :
- **Numéros de ligne** : `parsed.data[i]` correspond à la ligne `i + 1` du fichier tant qu'aucune cellule entre guillemets ne contient de saut de ligne. Accepté (cas marginal, noté en commentaire).
- **Une seule colonne** : PapaParse ajoute une erreur `type: 'Delimiter'` (`UndetectableDelimiter`) et parse avec `,` ; seule l'erreur `Quotes` est fatale. Le test « une seule colonne » le garantit.
- **`\u0000`** dans une chaîne littérale : si Sonar/oxlint le signale, utiliser `'|'` (un `|` dans un nom ne produirait au pire qu'un faux doublon signalé).
- Le test des doublons attend `name: 'lefevre CHLOE'` (valeurs trimées de la ligne en double, telles qu'écrites).
- Complexité cognitive (Sonar) : si `parseStudentsCsv` dépasse le seuil, extraire `readDataRows(dataRows, columns)`.

- [ ] **Step 6:** relancer → PASS.

- [ ] **Step 7: messages (test qui échoue)** — `src/students/messages.test.ts` : un objet `SAMPLE: CsvIssueParams` (params réels par code) ; pour chaque locale de `SUPPORTED_LOCALES`, un `expect` explicite par code (6 codes) que `formatCsvIssue` renvoie un texte non vide ; un test que `formatCsvIssue(csvWarning('preamble_skipped', { count: 2 }), 'fr')` contient `2`. Même forme que `src/backup/messages.test.ts` (sans `as`, sans boucle sur `Object.keys`).

- [ ] **Step 8: `messages.ts`**

```ts
import { t, type Dictionary, type Locale } from '../i18n'
import type { CsvIssue, CsvIssueParams } from './issues'

const fr: Dictionary<CsvIssueParams> = {
  csv_syntax: () => "Guillemet non fermé : la suite du fichier ne peut pas être lue.",
  no_students: () => 'Aucun étudiant valide dans ce fichier (il faut un nom et un prénom par ligne).',
  preamble_skipped: ({ count }) =>
    `${count} ligne${count > 1 ? 's' : ''} avant l'en-tête ignorée${count > 1 ? 's' : ''}.`,
  single_field_row: () => 'Nom ou prénom manquant : ligne ignorée.',
  extra_columns: ({ count }) =>
    `Colonnes en trop sur ${count} ligne${count > 1 ? 's' : ''} : seules les colonnes nom et prénom sont lues.`,
  duplicate_student: ({ firstLine, name }) => `${name} figure déjà ligne ${firstLine}.`,
}

const en: Dictionary<CsvIssueParams> = {
  csv_syntax: () => 'Unclosed quote: the rest of the file cannot be read.',
  no_students: () => 'No valid student in this file (each line needs a last name and a first name).',
  preamble_skipped: ({ count }) => `${count} line${count > 1 ? 's' : ''} before the header ignored.`,
  single_field_row: () => 'Missing last name or first name: line ignored.',
  extra_columns: ({ count }) =>
    `Extra columns on ${count} line${count > 1 ? 's' : ''}: only the last name and first name columns are read.`,
  duplicate_student: ({ firstLine, name }) => `${name} already appears on line ${firstLine}.`,
}

export const CSV_ISSUE_MESSAGES: Record<Locale, Dictionary<CsvIssueParams>> = { fr, en }

/** Message seul ; l'UI préfixe « Ligne N : » quand `issue.line` est défini. */
export function formatCsvIssue(issue: CsvIssue, locale: Locale): string {
  return t(CSV_ISSUE_MESSAGES, locale, issue.code, issue.params)
}
```

(Si `t(…, issue.code, issue.params)` ne type pas sur l'union distributive, reprendre la solution de `src/backup/messages.ts`. Les ternaires de pluriel ne sont pas imbriqués. Si Sonar refuse deux ternaires dans un gabarit, extraire `const plural = (n: number, s: string) => (n > 1 ? s : '')`.)

- [ ] **Step 9: `index.ts`** — réexporte `parseStudentsCsv`, `CsvStudent`, `CsvParseResult`, `CsvIssue`, `CsvIssueCode`, `CsvIssueParams`, `formatCsvIssue`, `CSV_ISSUE_MESSAGES`.

- [ ] **Step 10:** tests → PASS ; `pnpm check` vert.

- [ ] **Step 11: commit** — `✨ Ajoute la lecture de la liste d'étudiants CSV` (inclure `package.json`, `pnpm-lock.yaml`).

---

### Task 2: CSV d'exemple publié et lien depuis l'accueil

**Files:**
- Create: `examples/students.example.csv`, `src/students/example.test.ts`
- Modify: `vite/config-schema-plugin.ts`, `vite/config-schema-plugin.test.ts`, `src/home/EmptyState.tsx`, `src/home/HomePage.test.tsx`, `src/i18n/ui-messages.ts` (+ son test si les clés y sont listées), `README.md`

**Interfaces:**
- Consumes: `parseStudentsCsv` (Task 1).
- Produces: `STUDENTS_EXAMPLE_FILE_NAME = 'students.example.csv'` exporté par le plugin ; clé `empty_students_example_link` dans `UiMessageParams` (fr : « Télécharger la liste d'étudiants d'exemple », en : « Download the example student list »).

- [ ] **Step 1: fichier d'exemple** — `examples/students.example.csv`, **UTF-8 avec BOM**, fins de ligne CRLF, séparateur `;` :

```
Nom;Prénom
Durand;Alice
Lefèvre;Chloé
Martin;Bruno
Nguyễn;Minh
Petit;Élodie
Robert;David
Moreau;Inès
Fontaine;Hugo
Mercier;Zoé
Girard;Théo
```

Écrire le fichier avec un outil qui garantit le BOM et les CRLF (ex. `printf '\xEF\xBB\xBF' > …` puis ajout des lignes en `\r\n`), puis vérifier avec `head -c 3 examples/students.example.csv | xxd` (`efbbbf`) et `file` (CRLF). Vérifier que Prettier ignore les `.csv` (il ne les formate pas).

- [ ] **Step 2: test qui échoue** — `src/students/example.test.ts` (environnement node : `// @vitest-environment node` en tête, lecture via `readFile(new URL('../../examples/students.example.csv', import.meta.url), 'utf8')`) : `parseStudentsCsv(text)` donne `issues: []` et 10 étudiants, le premier `{ lastName: 'Durand', firstName: 'Alice', line: 2 }`, et le texte commence par le BOM.

- [ ] **Step 3: plugin** — dans `vite/config-schema-plugin.ts` :
  - `export const STUDENTS_EXAMPLE_FILE_NAME = 'students.example.csv'` et `const STUDENTS_EXAMPLE_FILE = fileURLToPath(new URL('../examples/students.example.csv', import.meta.url))`.
  - `matchConfigAsset` reconnaît aussi ce nom.
  - `renderConfigAssets` lit aussi le CSV (`readFile(…, 'utf8')`, BOM conservé) et l'ajoute à `watchFiles` ; il renvoie `studentsExample`.
  - Middleware : `Content-Type` `text/csv; charset=utf-8` pour le CSV, `application/json; charset=utf-8` pour les deux autres (une table `nom → { contenu, type }` plutôt qu'un ternaire imbriqué).
  - `generateBundle` émet aussi `students.example.csv`.
  - Commentaire de tête et JSDoc mis à jour (« Publie config.schema.json, config.example.json et students.example.csv… », D17, D56).

  Tests (`vite/config-schema-plugin.test.ts`) : `matchConfigAsset` reconnaît `students.example.csv` ; `renderConfigAssets` renvoie `studentsExample` identique au fichier (BOM compris) et l'inclut dans `watchFiles`. Si le fichier teste déjà le middleware ou `generateBundle`, étendre ces tests au CSV (type `text/csv`, asset émis).

- [ ] **Step 4: accueil** — ajouter la clé `empty_students_example_link` (fr/en) ; dans `EmptyState.tsx`, un second lien `<a href={`${import.meta.env.BASE_URL}students.example.csv`} download …>` à côté du lien de config (même style, les deux sur une ligne `flex flex-wrap justify-center gap-x-4`). Dans `HomePage.test.tsx`, le test de l'état vide vérifie aussi ce lien (`href` finissant par `students.example.csv`, attribut `download`).

- [ ] **Step 5: README** — dans « Usage prévu », étape 1 : mentionner la liste d'exemple publiée (`https://adriengras.github.io/questionator-z4000-hyperdrive/students.example.csv`, aussi dans `examples/students.example.csv`), comme pour la config.

- [ ] **Step 6:** tests ciblés (`src/students`, `vite`, `src/home`, `src/i18n`) → PASS ; `pnpm check` et `pnpm build` verts ; vérifier que `dist/students.example.csv` existe et commence par le BOM (`head -c 3 dist/students.example.csv | xxd`).

- [ ] **Step 7: commit** — `✨ Publie une liste d'étudiants d'exemple`

---

### Task 3: construction de la session et nom par défaut

**Files:**
- Create: `src/sessions/build-session.ts`, `src/sessions/build-session.test.ts`, `src/create/default-session-name.ts`, `src/create/default-session-name.test.ts`

**Interfaces:**
- Consumes: `Session` (`../domain/types`), `NormalizedConfig` (`../config/normalize`), `APP_VERSION` (`../app-version`), `SessionSchema` (tests), `checkSessionRules` (tests, `../backup/rules`), `Locale`.
- Produces:
  - `type BuildSessionInput = { name: string; examiner: string; config: NormalizedConfig; students: readonly { lastName: string; firstName: string }[] }`
  - `type BuildSessionDeps = { newId: () => string; now: () => Date }`
  - `buildSession(input: BuildSessionInput, deps: BuildSessionDeps): Session`
  - `defaultSessionName(examTitle: string, now: Date, locale: Locale): string`

- [ ] **Step 1: tests qui échouent** — `src/sessions/build-session.test.ts` :

```ts
import { describe, expect, test } from 'vitest'
import { checkSessionRules } from '../backup/rules'
import { SessionSchema } from '../domain/schema'
import { makeConfig } from '../test/student-fixtures'
import { APP_VERSION } from '../app-version'
import { buildSession } from './build-session'

function sequentialIds() {
  let n = 0
  return () => {
    n += 1
    return `id-${n}`
  }
}

const NOW = new Date('2026-09-25T10:00:00.000Z')

function build(overrides: Partial<Parameters<typeof buildSession>[0]> = {}) {
  return buildSession(
    {
      name: '  Oral PHP  ',
      examiner: '  M. Dupont ',
      config: makeConfig(),
      students: [
        { lastName: 'Durand', firstName: 'Alice' },
        { lastName: 'Martin', firstName: 'Bruno' },
      ],
      ...overrides,
    },
    { newId: sequentialIds(), now: () => NOW },
  )
}

describe('buildSession', () => {
  test('session complète, étudiants dans l’ordre du CSV', () => {
    const config = makeConfig()
    expect(build({ config })).toStrictEqual({
      id: 'id-1',
      name: 'Oral PHP',
      examiner: 'M. Dupont',
      createdAt: '2026-09-25T10:00:00.000Z',
      updatedAt: '2026-09-25T10:00:00.000Z',
      appVersion: APP_VERSION,
      config,
      students: [
        { id: 'id-2', lastName: 'Durand', firstName: 'Alice', order: 1, addedDuringSession: false, absent: false, attempts: [] },
        { id: 'id-3', lastName: 'Martin', firstName: 'Bruno', order: 2, addedDuringSession: false, absent: false, attempts: [] },
      ],
      activeStudentId: 'id-2',
      projection: { mode: 'waiting' },
    })
  })

  test('config figée = l’objet normalisé fourni (même référence)', () => {
    const config = makeConfig()
    expect(build({ config }).config).toBe(config)
  })

  test('examinateur vide ou blanc : clé absente', () => {
    expect('examiner' in build({ examiner: '   ' })).toBe(false)
  })

  test('valide pour le schéma et les règles croisées du backup', () => {
    const session = build()
    expect(SessionSchema.safeParse(session).success).toBe(true)
    expect(checkSessionRules(session)).toEqual([])
  })

  test('sans étudiant : lève', () => {
    expect(() => build({ students: [] })).toThrow()
  })
})
```

`src/create/default-session-name.test.ts` : `defaultSessionName('Oral PHP', new Date(2026, 8, 25, 12), 'fr')` → `'Oral PHP — 25 septembre 2026'` ; en `'en'` → `'Oral PHP — September 25, 2026'`.

- [ ] **Step 2:** `./node_modules/.bin/vitest run src/sessions src/create/default-session-name.test.ts` → FAIL.

- [ ] **Step 3: implémentation**

```ts
// src/sessions/build-session.ts
import { APP_VERSION } from '../app-version'
import type { NormalizedConfig } from '../config/normalize'
import type { Session, Student } from '../domain/types'

export type BuildSessionInput = {
  name: string
  examiner: string
  config: NormalizedConfig
  students: readonly { lastName: string; firstName: string }[]
}

export type BuildSessionDeps = { newId: () => string; now: () => Date }

/**
 * Session fraîche (F06) : config normalisée figée, étudiants dans l'ordre du CSV, premier
 * étudiant actif, projection en attente. Examinateur vide → clé absente (D11).
 */
export function buildSession(input: BuildSessionInput, deps: BuildSessionDeps): Session {
  if (input.students.length === 0) throw new Error('Une session exige au moins un étudiant.')
  const timestamp = deps.now().toISOString()
  const id = deps.newId()
  const students: Student[] = input.students.map((student, index) => ({
    id: deps.newId(),
    lastName: student.lastName,
    firstName: student.firstName,
    order: index + 1,
    addedDuringSession: false,
    absent: false,
    attempts: [],
  }))
  const examiner = input.examiner.trim()
  return {
    id,
    name: input.name.trim(),
    ...(examiner !== '' && { examiner }),
    createdAt: timestamp,
    updatedAt: timestamp,
    appVersion: APP_VERSION,
    config: input.config,
    students,
    activeStudentId: students[0]?.id,
    projection: { mode: 'waiting' },
  }
}
```

(Ordre des clés : `toStrictEqual` ne dépend pas de l'ordre. `activeStudentId: students[0]?.id` est typé `string | undefined` ; la garde en tête garantit la présence — si le typage de `Session` refuse une valeur `undefined` explicite avec `exactOptionalPropertyTypes`, lire `students[0]` dans une variable et lever si absente.)

```ts
// src/create/default-session-name.ts
import type { Locale } from '@/i18n'

/** `<exam.title> — <date longue localisée>` (F06, D57). */
export function defaultSessionName(examTitle: string, now: Date, locale: Locale): string {
  return `${examTitle} — ${new Intl.DateTimeFormat(locale, { dateStyle: 'long' }).format(now)}`
}
```

- [ ] **Step 4:** tests → PASS ; `pnpm check` vert.

- [ ] **Step 5: commit** — `✨ Ajoute la construction d'une session fraîche`

---

### Task 4: bandeau d'état partagé, zone de dépôt et hook du formulaire

**Files:**
- Create: `src/components/DbStatusBanner.tsx`, `src/create/FileDropField.tsx`, `src/create/FileDropField.test.tsx`, `src/create/use-create-form.ts`, `src/create/use-create-form.test.ts`
- Modify: `src/home/SessionList.tsx` (utilise `DbStatusBanner`), `src/i18n/ui-messages.ts` (+ test)

**Interfaces:**
- Consumes: `parseStudentsCsv`, `CsvParseResult` (`@/students`) ; `buildSession` (`@/sessions/build-session`) ; `defaultSessionName` (`./default-session-name`) ; `createSession`, `requestPersistentStorage`, `type DbStatus` (`@/db`) ; `type ValidationResult` (`@/config/validate`, **import de type seulement**) ; `useUi`, `type Ui`.
- Produces:
  - `<DbStatusBanner ui status />` : rend le bandeau `outdated` (texte `db_outdated` + bouton `db_reload`) ou `unavailable` (`db_unavailable`), `null` si `open`. Balisage identique à celui de `SessionList` aujourd'hui (les tests de l'accueil doivent rester verts sans changement).
  - `<FileDropField ui label accept fileName status onFile disabled? />` avec `status: 'empty' | 'reading' | 'ok' | 'warnings' | 'errors'` et `onFile: (file: File) => void`.
  - `useCreateForm(locale: Locale)` → voir Step 3.
  - Toutes les clés d'interface de l'écran (liste ci-dessous), utilisées par cette tâche et la suivante.

**Clés à ajouter à `UiMessageParams`** (fr / en) — la tâche 5 les consomme telles quelles :

| Clé | Params | fr | en |
|---|---|---|---|
| `create_title` | — | Nouvelle session | New session |
| `create_students_label` | — | Liste d'étudiants (CSV) | Student list (CSV) |
| `create_config_label` | — | Configuration (JSON) | Configuration (JSON) |
| `create_choose_file` | — | Choisir un fichier | Choose a file |
| `create_replace_file` | — | Remplacer | Replace |
| `create_drop_hint` | — | ou déposez-le ici | or drop it here |
| `create_file_status_ok` | — | Fichier valide | Valid file |
| `create_file_status_warnings` | — | Fichier valide, avec avertissements | Valid file, with warnings |
| `create_file_status_errors` | — | Fichier invalide | Invalid file |
| `create_students_example_link` | — | Télécharger la liste d'exemple | Download the example list |
| `create_validator_load_error` | — | La validation n'a pas pu démarrer. Rechargez la page. | Validation could not start. Reload the page. |
| `create_submit` | — | Créer la session | Create session |
| `create_write_error` | — | La création a échoué. Réessayez. | Creation failed. Please try again. |
| `create_preview_title` | — | Aperçu | Preview |
| `create_preview_empty` | — | Déposez une liste d'étudiants et une configuration pour voir l'aperçu. | Drop a student list and a configuration to see the preview. |
| `preview_students_count` | `{ count }` | `N étudiant(s)` (pluriel) | `N student(s)` |
| `preview_students_list` | — | Voir la liste | Show the list |
| `preview_line` | `{ line, message }` | `Ligne ${line} : ${message}` | `Line ${line}: ${message}` |
| `preview_config_title` | — | Configuration | Configuration |
| `preview_subject` | `{ subject }` | `Matière : …` | `Subject: …` |
| `preview_cohort` | `{ cohort }` | `Promotion : …` | `Cohort: …` |
| `preview_category` | `{ label, questions, scale }` | `${label} : ${questions} question(s), barème ${scale}` | `${label}: ${questions} question(s), scale ${scale}` |
| `preview_scoring` | `{ questionsPerStudent, maxRawScore, finalScale }` | `${q} question(s) par étudiant · note brute sur ${max} → note finale sur ${final}` | `${q} question(s) per student · raw score out of ${max} → final score out of ${final}` |
| `preview_rounding` | `{ mode: 'nearest' \| 'up' \| 'down'; step: number \| null; decimals: number }` | « Arrondi au plus proche / supérieur / inférieur, au pas de 0,5 » ou « …, à 2 décimales » | « Rounded to nearest / up / down, step 0.5 » or « …, 2 decimals » |
| `preview_skips_disabled` | — | Skips désactivés | Skips disabled |
| `preview_skips` | `{ max }` | `Skips autorisés : ${max} par étudiant` | `Skips allowed: ${max} per student` |

Réutiliser les clés existantes : `back_home`, `rename_label` (nom de la session), `examiner_label`, `examiner_hint`, `empty_example_link` (config d'exemple), `import_read_error` (lecture de fichier), `db_*`. Les nombres affichés dans `preview_rounding` utilisent `toLocaleString('fr')` / `toLocaleString('en')` dans chaque dictionnaire ; pluriels via un petit helper, sans ternaire imbriqué (pour le mode, une table `{ nearest: 'au plus proche', up: 'supérieur', down: 'inférieur' }`).

- [ ] **Step 1: `DbStatusBanner`** — extraire de `src/home/SessionList.tsx` les deux branches `outdated` / `unavailable` dans `src/components/DbStatusBanner.tsx` (`Readonly<{ ui: Ui; status: DbStatus }>`, renvoie `null` si `open`) ; `SessionList` fait `if (status !== 'open') return <DbStatusBanner ui={ui} status={status} />`. Lancer `./node_modules/.bin/vitest run src/home` → toujours vert.

- [ ] **Step 2: clés** — ajouter les clés du tableau (fr/en) ; le test du dictionnaire (`ui-messages.test.ts`) doit couvrir les nouvelles clés via son objet d'exemple ; ajouter un test du pluriel fr de `preview_students_count` (1 / 2) et des deux formes de `preview_rounding` (pas / décimales).

- [ ] **Step 3: hook, tests qui échouent** — `src/create/use-create-form.ts` expose :

```ts
export type FileSlot<T> =
  | { kind: 'empty' }
  | { kind: 'reading'; fileName: string }
  | { kind: 'read-error'; fileName: string }
  | { kind: 'load-error'; fileName: string } // config : échec du chargement du validateur
  | { kind: 'loaded'; fileName: string; result: T }

export type CreateForm = {
  students: FileSlot<CsvParseResult>
  config: FileSlot<ValidationResult>
  name: string
  examiner: string
  submitting: boolean
  submitError: boolean
  canSubmit: boolean
  setStudentsFile: (file: File) => Promise<void>
  setConfigFile: (file: File) => Promise<void>
  setName: (value: string) => void // saisie utilisateur : arrête le préremplissage
  setExaminer: (value: string) => void
  submit: () => Promise<string | undefined> // id de la session créée, undefined si échec ou refus
}

export function useCreateForm(locale: Locale, dbStatus: DbStatus): CreateForm
```

Règles :
- `setStudentsFile` : `reading` → `file.text()` (échec → `read-error`) → `parseStudentsCsv` → `loaded`.
- `setConfigFile` : `reading` → `file.text()` (échec → `read-error`) → `const { validateConfig } = await import('@/config/validate')` (échec → `load-error`) → `validateConfig(text, { cssSupports: (p, v) => CSS.supports(p, v) })` → `loaded`. Si `result.ok` et que le nom n'a jamais été saisi à la main : `name = defaultSessionName(result.config.exam.title, new Date(), locale)`.
- Réponse tardive : si un second fichier est déposé dans le même emplacement avant la fin du premier, seul le dernier compte (compteur de séquence par emplacement, comme `persistence.ts`).
- `canSubmit` = étudiants `loaded` sans issue `error` **et** config `loaded` avec `result.ok` **et** `name.trim() !== ''` **et** `dbStatus === 'open'` **et** `!submitting`.
- `submit` : si `!canSubmit` ou déjà en cours (garde synchrone par `useRef`, pour qu'un double clic ne crée qu'une session) → `undefined`. Sinon `submitting = true`, `submitError = false` ; `await requestPersistentStorage().catch(() => false)` (un refus ou une erreur ne bloque pas) ; `buildSession({ name, examiner, config: result.config, students }, { newId: () => crypto.randomUUID(), now: () => new Date() })` ; `await createSession(session)` ; succès → renvoie `session.id` (laisser `submitting` à `true` : la page navigue) ; échec → `submitError = true`, `submitting = false`, `undefined`.

Tests (`renderHook`, `fake-indexeddb`, `vi.stubGlobal('CSS', { supports: () => true })`, fichiers via `new File([text], 'x.csv')`) :
- état initial : deux emplacements `empty`, `canSubmit` faux ;
- CSV avec avertissements seuls + config valide → `canSubmit` vrai ; CSV `no_students` → faux ; config invalide → faux ;
- nom prérempli au chargement d'une config valide (`'Oral de test — 25 septembre 2026'` avec `vi.setSystemTime`, ou vérifier le préfixe `'Oral de test — '`) ;
- après `setName('Mon oral')`, charger une autre config valide ne change pas le nom ; `setName('')` puis config valide : le nom reste vide ;
- remplacer une config valide par une invalide : nom conservé, `canSubmit` faux (Review Focus 3) ;
- `dbStatus = 'outdated'` → `canSubmit` faux ;
- `submit` → renvoie un id, `getSession(id)` : nom trimé, config = `result.config` (toStrictEqual), étudiants dans l'ordre, `activeStudentId` = premier ;
- double appel de `submit` sans attendre → une seule session en base (`db.sessions.count()` = 1) (Review Focus 4) ;
- échec de `createSession` (`vi.spyOn` sur le module ou `vi.mock('@/db', …)` partiel qui rejette) → `submitError` vrai, `submitting` faux, formulaire conservé ;
- `requestPersistentStorage` qui rejette → la session est quand même créée ;
- lecture de fichier en échec (`File` dont `text()` rejette : objet `{ name: 'x.csv', text: () => Promise.reject(new Error()) }` typé via une fonction qui renvoie `File`, sans `as` — ou `vi.spyOn(file, 'text').mockRejectedValue(...)`) → `read-error`.

- [ ] **Step 4: implémenter le hook** jusqu'au vert.

- [ ] **Step 5: `FileDropField`** — zone `Card`/`div` à bordure pointillée (`border-dashed`, `border-primary` pendant le survol), `role="group"` + `aria-label={label}` :
  - `label` (texte visible), input `type="file"` caché (`accept`, `onChange` → `onFile(files[0])` puis `value = ''`), bouton `create_choose_file` (ou `create_replace_file` si `fileName`) qui ouvre l'input ; texte `create_drop_hint` ;
  - si `fileName` : nom du fichier + icône Tabler selon `status` (`IconCircleCheck` `ok`, `IconAlertTriangle` `warnings`, `IconCircleX` `errors`, `IconLoader2` animée `reading`) avec `aria-label` `create_file_status_*` ;
  - `onDragOver` : si fichiers et non `disabled` → `preventDefault()`, survol actif ; si `disabled` → `preventDefault()` + `dropEffect = 'none'` ; `onDragLeave` → survol inactif ; `onDrop` → `preventDefault()`, `onFile(dataTransfer.files[0])` si présent et non `disabled`.

  Tests (`FileDropField.test.tsx`) : clic sur « Choisir un fichier » ouvre l'input (spy `click`) ; `change` appelle `onFile` et remet `value` à `''` ; `drop` appelle `onFile` avec le premier fichier ; `dragOver` renvoie `false` (preventDefault) même `disabled`, et `drop` ne rappelle pas `onFile` si `disabled` ; icône d'état avec le bon libellé accessible.

- [ ] **Step 6:** `./node_modules/.bin/vitest run src/create src/home src/i18n src/components` → PASS ; `pnpm check` vert.

- [ ] **Step 7: commit** — `✨ Ajoute le formulaire de création (état, zones de dépôt, bandeau partagé)`

---

### Task 5: écran de création et route `/new`

**Files:**
- Create: `src/create/CreateSessionPage.tsx`, `src/create/StudentsPreview.tsx`, `src/create/ConfigPreview.tsx`, `src/create/CreateSessionPage.test.tsx`
- Modify: `src/routes/new.tsx`, `src/routes/routes.test.tsx` (le test `/new` n'attend plus « Bientôt disponible »), éventuellement `src/test/render-home.tsx` → helper générique `renderAt(path)` partagé

**Interfaces:**
- Consumes: `useCreateForm`, `FileDropField`, `DbStatusBanner` (Task 4) ; `formatCsvIssue` (`@/students`) ; `formatConfigIssue` (`@/config/messages`), `formatPath` (`@/config/issues`) ; `useDbStatus` (`@/db`) ; `useUi` ; `useNavigate` / `Link` (TanStack Router).
- Produces: `<CreateSessionPage />` monté par `createFileRoute('/new')({ component: CreateSessionPage })`.

- [ ] **Step 1: tests d'écran qui échouent** — `src/create/CreateSessionPage.test.tsx`, monté via le routeur sur `/new` (`createAppRouter(createMemoryHistory({ initialEntries: ['/new'] }))`), `fake-indexeddb`, `vi.stubGlobal('CSS', { supports: () => true })`, `vi.mock('@/db', …)` partiel pour piloter `useDbStatus` (comme `HomePage.test.tsx`). Fichiers déposés via `fireEvent.change` sur les inputs (`container.querySelectorAll('input[type=file]')`, CSV d'abord) ou `fireEvent.drop` sur la zone (`getByRole('group', { name: 'Liste d'étudiants (CSV)' })`). Config valide : `JSON.stringify(minimalConfig())` (`src/test/config-fixtures.ts`) ; config invalide : `JSON.stringify({ ...minimalConfig(), categories: [] })` et une config à plusieurs erreurs (par ex. `exam` manquant et `scoring.finalScale` négatif). Tests :
  - titre « Nouvelle session », lien « Retour à l'accueil » vers `/`, liens d'exemple (`students.example.csv`, `config.example.json`, attribut `download`) ;
  - sans fichier : « Créer la session » désactivé, texte d'aide de l'aperçu ;
  - CSV valide + config valide → aperçu « 2 étudiants », titre de l'épreuve, une ligne de catégorie, règles de notation ; nom prérempli ; bouton actif ;
  - config invalide à plusieurs erreurs → **toutes** affichées, chacune avec son chemin (`<code>`), bouton désactivé ;
  - CSV avec une ligne à un champ → « Ligne 3 : Nom ou prénom manquant : ligne ignorée. », bouton actif (avertissement seul) ;
  - CSV sans étudiant → erreur affichée, bouton désactivé ;
  - config valide avec avertissement (icône inconnue sur une catégorie) → avertissement affiché, bouton actif (Review Focus 5) ;
  - création → `await screen.findByRole('heading', { name: 'Bientôt disponible' })` (page provisoire de `/session/$sessionId`) et une session en base avec les étudiants du CSV ;
  - config figée : après création, `db.sessions.toArray()` puis vérifier que `config` égale `normalize`/`validateConfig` de la config déposée — et qu'un rechargement de l'écran avec une autre config ne modifie pas la session existante ;
  - échec d'écriture (`createSession` mocké qui rejette) → « La création a échoué. Réessayez. », champs conservés, bouton réactivé ;
  - `outdated` → bandeau « Rechargez la page », bouton désactivé ;
  - en anglais (`navigator.languages = ['en-US']`, restauré après) → « New session ».

- [ ] **Step 2:** `./node_modules/.bin/vitest run src/create/CreateSessionPage.test.tsx` → FAIL.

- [ ] **Step 3: composants**

`CreateSessionPage.tsx` :

```tsx
export function CreateSessionPage() {
  const ui = useUi()
  const { text, locale } = ui
  const status = useDbStatus()
  const form = useCreateForm(locale, status)
  const navigate = useNavigate()

  async function handleSubmit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault()
    const id = await form.submit()
    if (id !== undefined) await navigate({ to: '/session/$sessionId', params: { sessionId: id } })
  }

  return (
    <main className="mx-auto flex min-h-svh max-w-5xl flex-col gap-6 p-4 sm:p-6">
      <header className="flex flex-col gap-2">
        <Link to="/" className="text-sm text-primary underline underline-offset-4">{text('back_home', {})}</Link>
        <h1 className="text-2xl font-bold tracking-tight">{text('create_title', {})}</h1>
      </header>
      <DbStatusBanner ui={ui} status={status} />
      <div className="grid gap-6 md:grid-cols-2">
        <form onSubmit={(event) => void handleSubmit(event)} className="flex flex-col gap-4">
          {/* deux FileDropField, liens d'exemple, nom (rename_label), examinateur (examiner_label + examiner_hint), erreur create_write_error en role="alert", bouton submit */}
        </form>
        <section aria-labelledby={previewTitleId} className="flex flex-col gap-4">
          {/* h2 create_preview_title ; create_preview_empty si les deux emplacements sont vides ; StudentsPreview ; ConfigPreview */}
        </section>
      </div>
    </main>
  )
}
```

- Statut d'un `FileDropField` dérivé de l'emplacement : `reading` → `'reading'` ; `read-error` / `load-error` → `'errors'` (+ message `import_read_error` / `create_validator_load_error` sous la zone) ; `loaded` → `'errors'` si une issue `error` (CSV) ou `!result.ok` (config), sinon `'warnings'` si des issues, sinon `'ok'` ; `empty` → `'empty'`. Placer cette dérivation dans une fonction pure `slotStatus(...)` testée (dans `use-create-form.ts` ou un petit module `slot-status.ts`).
- Champs nom et examinateur : `Label` + `Input` (`useId`), `onChange` → `form.setName` / `form.setExaminer`.
- Bouton `type="submit"` `disabled={!form.canSubmit}`.

`StudentsPreview.tsx` (`Readonly<{ ui: Ui; result: CsvParseResult }>`) : `h3` `preview_students_count`, `<details><summary>preview_students_list</summary><ol>` des « Nom Prénom » (clé `line`), puis `<ul>` des issues (erreurs d'abord), chaque ligne : `issue.line !== undefined` → `preview_line({ line, message })`, sinon le message seul ; erreurs en `text-destructive`, avertissements en `text-muted-foreground` avec `IconAlertTriangle`.

`ConfigPreview.tsx` (`Readonly<{ ui: Ui; result: ValidationResult }>`) : si `result.ok` : `h3` titre de l'épreuve, `preview_subject` / `preview_cohort` si présents, `<ul>` des catégories (`preview_category` avec `questions = category.questions.length`, `scale = category.scale.join(', ')`), `preview_scoring`, `preview_rounding` (depuis `config.scoring.rounding`), skips (`preview_skips` si `enabled`, sinon `preview_skips_disabled`). Puis toutes les issues (erreurs d'abord) : `<code>{formatPath(issue.path)}</code>` (si non vide) + `formatConfigIssue(issue, locale)`. Clé React : `${formatPath(issue.path)}|${issue.code}` suffixée d'un compteur d'occurrence (pas d'index seul, Sonar S6479).

`src/routes/new.tsx` : `createFileRoute('/new')({ component: CreateSessionPage })`. Mettre à jour `routes.test.tsx` : `/new` affiche le heading « Nouvelle session » ; `/session/abc` affiche toujours « Bientôt disponible ». `ComingSoon` reste utilisé par `/session/$sessionId`.

- [ ] **Step 4:** tests → PASS ; `pnpm check` et `pnpm build` verts, sans avertissement de taille de chunk. Relever dans le rapport la taille du chunk de `/new` et celle du chunk contenant `validate`, et vérifier (grep dans `dist/assets`) que le chunk du validateur n'embarque pas les composants d'icônes Tabler (chercher par ex. `createReactComponent` ou `IconAccessible`), seulement la liste des noms.

- [ ] **Step 5: commit** — `✨ Ajoute l'écran de création de session`

---

## Après les tâches (contrôleur)

- Revue finale de branche, vague de corrections unique.
- Vérification manuelle Chromium sur `pnpm build` + `vite preview --port 4173` (jamais `pnpm dev`) : fichiers d'exemple, CSV avec préambule et lignes anormales, config invalide, retour à l'accueil.
- Mémoire projet : `docs/INDEX.md`, `docs/HANDOFF.md`, `docs/QUIRKS.md`, `docs/CONVENTIONS.md` (squelette « Lecture de fichier déposé » si le motif `FileSlot` est réutilisable), `docs/BACKLOG.md` (retirer l'entrée « Vérifier en F06 que le chunk… » si vérifiée).
- PR brouillon `Closes #6`, `sonar-check.sh --pr <n> --wait`, puis « Ready for review ».
